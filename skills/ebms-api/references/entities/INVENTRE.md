<!-- GENERATED by ebms-openapi/src/skill-refs.js — do not edit by hand.
     Regenerate: cd ebms-openapi && npm run skill-refs -- --out=<this skill's directory>
     Hand-written knowledge belongs in SKILL.md, entities.md or odata-quirks.md instead. -->

# INVENTRE: Product category

Module: **Inventory › Product categories** · Entity type `INVENTRE` · Generated 2026-09-17 from EBMS 1.8.148, 2 installs.

Only fields and actions present on **every** install checked are listed. Your install may
have custom fields on top — look those up live rather than guessing:
`GET {base}/EntityMetaData('INVENTRE')?$expand=Properties` (no credentials needed on most installs).

## Keys and addressing

- Natural key: `TREE_ID`
- Every row also has `AUTOID`, unique and unambiguous.
- Path forms EBMS accepts: `/INVENTRE('{AUTOID}')`, `/INVENTRE('{TREE_ID}')`
- EBMS reports a wrong key form as 404, 400 **or** 422. Try the forms in order — see
  **Key addressing** in `../odata-quirks.md` before writing a PATCH.

## Operations

| Operation | Request | Capability |
|---|---|---|
| List / query | `GET /INVENTRE` | verified |
| Create | `POST /INVENTRE` | unverified |
| Read one | `GET /INVENTRE('{AUTOID}')` | unverified |
| Update | `PATCH /INVENTRE('{AUTOID}')` | unverified |
| Delete | `DELETE /INVENTRE('{AUTOID}')` | unverified |

`verified` = confirmed against a live install and recorded by a person.
`unverified` = standard OData that EBMS has **not** been confirmed to support for this
entity. EBMS publishes no capability annotations, so treat these as untested: try them in
a sandbox company, and read the record back afterwards.

## Fields (27)

`?` = nullable · `string(24)` = max length · `decimal(2)` = scale · **required** = EBMS
requires it · **read-only** = computed or not writable · **select-only** = virtual field
omitted from a plain GET and from `$select=*`; name it explicitly in `$select`.

| Field | Type | Flags | EBMS label |
|---|---|---|---|
| `TREE_ID` | string(5)? | **required** | Tree ID |
| `AUTOID` | string(16)? | **read-only** | Auto ID — The ID that identifies the record in EBMS |
| `COLOR_BY` | int32 |  | Color by |
| `CRID_TYPE` | string(10)? |  | ID creation template type |
| `CRID_VALUE` | string(90)? |  | ID creation template — When creating new inventory items, a default ID will be created from this template. |
| `DEF_INVEN` | string(24)? |  | Default inventory |
| `Guid` | guid |  |  |
| `IsNewEntity` | bool | **select-only** | Is new entity — A virtual property that we can base computes off of to show if an entity is virtual |
| `KA_INFO` | string(15)? |  | Keystone auto info |
| `PARENT_ID` | string(5)? |  | Parent Folder ID — This field contains parent folder ID (TREE_ID) for this folder. If it is zero, then this is the root folder. |
| `ParentsDescription` | string(35)? | **select-only** | Parents description |
| `PR_NEWFLDR` | bool |  | PR New Folder |
| `PR_NEWITEM` | bool |  | PR New Item |
| `SHOW_WEB` | bool |  | Show on website |
| `SUB_ONLY` | bool |  | Allow only subcategories |
| `Subtitle` | string? | **read-only** |  |
| `TEMPLATE` | string(32)? |  | Template |
| `Title` | string? | **read-only** |  |
| `TREE_CLOSE` | string(2)? |  | Closed folder icon ID — The ID of the icon should be used for an closed folder. |
| `TREE_DESCR` | string(35)? |  | Description |
| `TREE_FLAGS` | string(9)? |  | Folder Attributes — The folder can have any combination of the following attributes: |
| `TREE_OPEN` | string(2)? |  | Open Folder Icon ID — The ID of the icon should be used for an open folder. |
| `TREE_PATH` | string(60)? |  | Folder tree path |
| `USE_C_IMGS` | bool |  | Use C Imgs |
| `WEB_DESCR1` | string(80)? |  | Web title |
| `WEB_DESCR2` | string? |  | Web short description |
| `WEB_DESCR3` | string? |  | Web long description |

## Related entities (7)

Reach these with `$expand=<name>`. For document detail rows, **always** read through the
parent this way: queried standalone, their `_VIS` quantity and cost fields read as 0.

| Expand | Target | Cardinality |
|---|---|---|
| `INATINVENTREs` | `INATINVENTRE` | many |
| `INVENMFs` | `INVENMF` | many |
| `INVENTREIMAGESs` | `INVENTREIMAGES` | many |
| `INVENTREs` | `INVENTRE` | many |
| `INVENTRESHOWWEBs` | `INVENTRESHOWWEB` | many |
| `INVENTRYs` | `INVENTRY` | many |
| `PARENT_ID_Reference` | `INVENTRE` | one |

## Bound actions (1)

Also bound here, as on most EBMS entities: `GetCustomizationDesignerMetadata`.
