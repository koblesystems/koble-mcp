<!-- GENERATED by ebms-openapi/src/skill-refs.js — do not edit by hand.
     Regenerate: cd ebms-openapi && npm run skill-refs -- --out=<this skill's directory>
     Hand-written knowledge belongs in SKILL.md, entities.md or odata-quirks.md instead. -->

# PYEMP: Worker

Module: **Labor › Workers** · Entity type `PYEMP` · Generated 2026-09-17 from EBMS 1.8.148, 2 installs.

Only fields and actions present on **every** install checked are listed. Your install may
have custom fields on top — look those up live rather than guessing:
`GET {base}/EntityMetaData('PYEMP')?$expand=Properties` (no credentials needed on most installs).

## Keys and addressing

- Natural key: `ID`
- Every row also has `AUTOID`, unique and unambiguous.
- Path forms EBMS accepts: `/PYEMP('{AUTOID}')`, `/PYEMP('{ID}')`
- EBMS reports a wrong key form as 404, 400 **or** 422. Try the forms in order — see
  **Key addressing** in `../odata-quirks.md` before writing a PATCH.

## Operations

| Operation | Request | Capability |
|---|---|---|
| List / query | `GET /PYEMP` | verified |
| Create | `POST /PYEMP` | unverified |
| Read one | `GET /PYEMP('{AUTOID}')` | unverified |
| Update | `PATCH /PYEMP('{AUTOID}')` | unverified |
| Delete | `DELETE /PYEMP('{AUTOID}')` | unverified |

`verified` = confirmed against a live install and recorded by a person.
`unverified` = standard OData that EBMS has **not** been confirmed to support for this
entity. EBMS publishes no capability annotations, so treat these as untested: try them in
a sandbox company, and read the record back afterwards.

## Verified notes

`TC_LOGIN` (the timeclock PIN) is returned **trimmed**, not space-padded to 10 as the underlying record stores it. Match as entered.

_From the human-maintained overlay/manual.json._

## Fields (55)

`?` = nullable · `string(24)` = max length · `decimal(2)` = scale · **required** = EBMS
requires it · **read-only** = computed or not writable · **select-only** = virtual field
omitted from a plain GET and from `$select=*`; name it explicitly in `$select`.

| Field | Type | Flags | EBMS label |
|---|---|---|---|
| `ID` | string(10)? | **required** **read-only** |  |
| `AUTOID` | string(16)? | **read-only** | Auto ID — The ID that identifies the record in EBMS |
| `ADDRESS1` | string(35)? |  | Address line 1 |
| `ADDRESS2` | string(35)? |  | Address line 2 |
| `ADJ_Q` | bool |  | Inventory counts can be adjusted |
| `APP_IDS` | string? |  | MyEbms Settings — JSON-serialized list of mobile UI settings for MyEbms. |
| `ASC_UPC` | bool |  | Inventory items can be associated with UPC's |
| `BIRTH_DATE` | datetime? | **read-only** | Birth date |
| `CITY` | string(30)? |  | City |
| `CONID_1` | string(15)? |  | Contact info ID 1 — Contact information ID 1 |
| `CONID_2` | string(15)? |  | Contact info ID 2 — Contact information ID 2 |
| `CONID_3` | string(15)? |  | Contact info ID 3 — Contact information ID 3 |
| `CONID_4` | string(15)? |  | Contact info ID 4 — Contact information ID 4 |
| `CONID_5` | string(15)? |  | Contact info ID 5 — Contact information ID 5 |
| `CONTACT_1` | string(40)? |  | Contact info 1 — Contact information field 1 |
| `CONTACT_2` | string(40)? |  | Contact info 2 — Contact information field 2 |
| `CONTACT_3` | string(40)? |  | Contact info 3 — Contact information field 3 |
| `CONTACT_4` | string(40)? |  | Contact info 4 — Contact information field 4 |
| `CONTACT_5` | string(40)? |  | Contact info 5 — Contact information field 5 |
| `COUNTRY` | string(25)? |  | Country |
| `DEF_PAY` | DefaultPay | **required** | Default pay |
| `EDIT_TMDET` | bool |  | Allow creation of tasks and editing of timecard lines. |
| `EVAL_DATE` | datetime? |  | Last evaluation |
| `EXTERNALID` | string(50)? |  | External ID |
| `F_NAME` | string(35)? |  | First name |
| `FSPTASKTYP` | string? |  | Field Service Pro Task Type |
| `FullName` | string(35)? | **select-only** | Full name |
| `Guid` | guid |  |  |
| `HEAD_HOUSE` | bool |  | Head of household |
| `HIRE_DATE` | datetime? | **required** | Hire date |
| `HR_PAY` | decimal(2) |  | Hourly pay |
| `INACTIVE` | bool |  | Inactive |
| `IsNewEntity` | bool | **select-only** | Is new entity — A virtual property that we can base computes off of to show if an entity is virtual |
| `JOB_CLK_IO` | bool |  | Clock into daily hours |
| `L_NAME` | string(35)? |  | Last name |
| `LastKnownLocation` | string(200)? | **select-only** | Last known location |
| `LastKnownLocationDateTime` | datetime? | **select-only** | Last known location date and time |
| `LOGIN_NAME` | string(20)? |  | Login name |
| `M_STATUS` | decimal(2) |  | Marital status |
| `ONLY_DAY` | bool |  | The first entry of the day always clock in |
| `PAY_METHOD` | PaymentMethod |  | Method — Payment method |
| `PAY_TYPE` | PayFrequency | **required** | Frequency |
| `PredefinedQueries` | string[]? | **select-only** | Predefined queries |
| `RAISE` | decimal(2) |  | Rate |
| `RAISE_DATE` | datetime? |  | Last raise |
| `SAL_PAY` | decimal(2) |  | Salary pay |
| `SEX` | decimal(2) |  | Gender |
| `SHOW_CALVW` | bool |  | Report location |
| `STATE` | string(3)? |  | State |
| `Subtitle` | string? | **read-only** |  |
| `TC_LOGIN` | string(10)? |  | TC Login |
| `Title` | string? | **read-only** |  |
| `TREE_ID` | string(5)? |  | Category ID |
| `TYPE` | string(20)? |  | Type |
| `ZIP` | string(11)? |  | Postal code |

## Related entities (13)

Reach these with `$expand=<name>`. For document detail rows, **always** read through the
parent this way: queried standalone, their `_VIS` quantity and cost fields read as 0.

| Expand | Target | Cardinality |
|---|---|---|
| `DispatchBoards` | `PYEMPDISPATCH` | many |
| `JCJBEMPs` | `JCJBEMP` | many |
| `PYCONTACTSs` | `PYCONTACTS` | many |
| `PYDLYHRSs` | `PYDLYHRS` | many |
| `PYEMPTASKQUERies` | `PYEMPTASKQUERY` | many |
| `PYEMPWORKCODEs` | `PYEMPWORKCODE` | many |
| `PYTMDETs` | `PYTMDET` | many |
| `PYTMDLHRs` | `PYTMDLHR` | many |
| `PYTMLOCATIONs` | `PYTMLOCATION` | many |
| `SchedulerLinks` | `PYEMPEMP` | many |
| `TAEMPs` | `TAEMP` | many |
| `TASKs` | `TASK` | many |
| `TREE_ID_Reference` | `PYEMPTRE` | one |

## Enumerations

- `DefaultPay`: `Salary` (1) · `Hourly` (2)
- `PayFrequency`: `Daily` (0) · `Weekly` (1) · `BiWeekly` (2) · `SemiMonthly` (3) · `Monthly` (4) · `BiMonthly` (5) · `Quarterly` (6) · `SemiAnnually` (7) · `Annually` (8)
- `PaymentMethod`: `Check` (0) · `DirectDeposit` (1) · `PayrollService` (2)

## Bound actions (6)

Invoked as `POST /PYEMP({key})/Model.Entities.<Action>`. Parameters and return types are what
the metadata declares, and every action here is **unverified** unless marked otherwise.

Most actions declare no parameters yet return a `…Dialog` type whose fields read like
inputs. That is EBMS's command-dialog pattern: clients have been seen posting those
fields as the request body (`INVENTRY.GetPrice` with `CustomerId` and `Quantity`). The
metadata does not confirm it, so treat the request shape as unknown until tested.

| Action | Parameters | Returns | Capability |
|---|---|---|---|
| `UpdateLocked` | _none declared_ | object | unverified |
| `ClockIn` | _none declared_ | PYEMPClockInCommandDialog { DateTime: datetime?, Force: bool, Memo: string?, TimeDetailAutoId: string(16)? } | unverified |
| `ClockOut` | _none declared_ | PYEMPClockOutCommandDialog { DateTime: datetime?, Memo: string?, TimeDetailAutoId: string(16)? } | unverified |
| `GetWorkerInfo` | _none declared_ | GetWorkerInfoDialog { CommandTitle: string(200)?, Worker: WorkerInfo } | unverified |
| `ClockedTimeDelete` | _none declared_ | PYEMPClockedTimeDeleteCommandDialog { DateTime: datetime?, In: bool? } | unverified |

Also bound here, as on most EBMS entities: `GetCustomizationDesignerMetadata`.
