<!-- GENERATED by ebms-openapi/src/skill-refs.js — do not edit by hand.
     Regenerate: cd ebms-openapi && npm run skill-refs -- --out=<this skill's directory>
     Hand-written knowledge belongs in SKILL.md, entities.md or odata-quirks.md instead. -->

# APINVDET: Purchase document detail

Module: **Purchasing › Purchase document lines** · Entity type `APINVDET` · Generated 2026-09-17 from EBMS 1.8.148, 2 installs.

Only fields and actions present on **every** install checked are listed. Your install may
have custom fields on top — look those up live rather than guessing:
`GET {base}/EntityMetaData('APINVDET')?$expand=Properties` (no credentials needed on most installs).

## Keys and addressing

- Natural key: `TIMESTAMP`
- Every row also has `AUTOID`, unique and unambiguous.
- Path forms EBMS accepts: `/APINVDET('{AUTOID}')`, `/APINVDET('{TIMESTAMP}')`
- EBMS reports a wrong key form as 404, 400 **or** 422. Try the forms in order — see
  **Key addressing** in `../odata-quirks.md` before writing a PATCH.

## Operations

| Operation | Request | Capability |
|---|---|---|
| List / query | `GET /APINVDET` | unverified |
| Create | `POST /APINVDET` | unverified |
| Read one | `GET /APINVDET('{AUTOID}')` | unverified |
| Update | `PATCH /APINVDET('{AUTOID}')` | unverified |
| Delete | `DELETE /APINVDET('{AUTOID}')` | unverified |

`verified` = confirmed against a live install and recorded by a person.
`unverified` = standard OData that EBMS has **not** been confirmed to support for this
entity. EBMS publishes no capability annotations, so treat these as untested: try them in
a sandbox company, and read the record back afterwards.

## Fields (38)

`?` = nullable · `string(24)` = max length · `decimal(2)` = scale · **required** = EBMS
requires it · **read-only** = computed or not writable · **select-only** = virtual field
omitted from a plain GET and from `$select=*`; name it explicitly in `$select`.

| Field | Type | Flags | EBMS label |
|---|---|---|---|
| `TIMESTAMP` | string(10)? | **required** **read-only** | Timestamp |
| `AUTOID` | string(16)? | **read-only** | Auto ID — The ID that identifies the record in EBMS |
| `ACCOUNT` | string(9)? |  | Account |
| `ATTRIBUTES` | string? |  | Attributes |
| `B_QUAN_VIS` | decimal(2) |  | Back ordered |
| `C_TYPE` | int32 |  | Classification — Product classification |
| `COST` | decimal(2) |  | Amount (Invoice) |
| `DESCR` | string? |  | Description |
| `DOC_AID` | string(16)? |  | Document auto ID |
| `DOC_LINKED` | string(40)? | **read-only** | Linked — List of linked documents |
| `DOC_TYPE` | DocumentType | **read-only** | Document type |
| `ETA_DATE` | datetime? |  | ETA date |
| `ETA_USER` | bool | **read-only** | ETA user |
| `Guid` | guid |  |  |
| `ID` | string(10)? |  | Customer ID |
| `INT_NOTE` | string? |  | Internal Note |
| `INV_DATE` | datetime? | **required** **read-only** | Invoice date |
| `INVEN` | string(24)? |  | Product ID |
| `INVOICE` | string(15)? | **read-only** | Invoice ID |
| `IsNewEntity` | bool | **select-only** | Is new entity — A virtual property that we can base computes off of to show if an entity is virtual |
| `JOB_ID` | string(10)? |  | Job ID |
| `JOBSTG_ID` | string(15)? |  | Job stage ID |
| `O_QUAN_VIS` | decimal(2) |  | Ordered — The number of this item used to make a single item of the parent type, in terms of the current unit of measure (the UNIT_MEAS field). |
| `O_SHIP_VIS` | decimal(2) |  | Shipped — The number of this item used to make a single item of the parent type, in terms of the current unit of measure (the UNIT_MEAS field). |
| `PART_NO` | string(24)? |  | Part number |
| `PO_AMOUNT` | decimal(2) |  | Amount (P.O.) |
| `PURC_M_VIS` | string(25)? | **read-only** | Purchase method |
| `RDATE` | datetime? |  | Received Date |
| `SHIP_DATE` | datetime? | **read-only** | Date shipped |
| `SHIP_VIS` | decimal(2) |  | Received — The total number of this item received, in terms of the currently selected unit of measure (in the UNIT_MEAS field). |
| `STATUS` | PurchaseInvoiceStatus | **read-only** | Status |
| `Subtitle` | string? | **read-only** |  |
| `Title` | string? | **read-only** |  |
| `UNIT` | decimal(2) |  | Unrounded Price — Unrounded unit price, not shown to the user |
| `UNIT_MEAS` | string(10)? |  | Measure — Unit of measure |
| `UNIT_VIS` | decimal(2) |  | Unit cost |
| `WAREHOUSE` | string(10)? |  | Warehouse ID |
| `WEIGHT` | decimal(2) |  | Weight |

## Related entities (13)

Reach these with `$expand=<name>`. For document detail rows, **always** read through the
parent this way: queried standalone, their `_VIS` quantity and cost fields read as 0.

| Expand | Target | Cardinality |
|---|---|---|
| `ConsumedDetails` | `ARINVDET` | many |
| `DOC_AID_Reference` | `APINV` | one |
| `DOC_AID_Reference_INMFG` | `INMFG` | one |
| `ID_Reference` | `ARCUST` | one |
| `INATINVDETs` | `INATINVDET` | many |
| `INTRANs` | `INTRAN` | many |
| `INVEN_Reference` | `INVENTRY` | one |
| `JOB_ID_Reference` | `JCJB` | one |
| `Lots` | `LotItem` | many |
| `SerializedItems` | `SerializedItem` | many |
| `UnitOfMeasure` | `INVENUNT` | one |
| `UnitOfMeasureItems` | `INVENUNT` | many |
| `WAREHOUSE_Reference` | `INWH` | one |

## Enumerations

- `DocumentType`: `None` (0) · `E` (1) · `S` (2) · `J` (3) · `W` (4) · `M` (5) · `B` (6) · `C` (7) · `D` (8)
- `PurchaseInvoiceStatus`: `PurchaseOrder` (0) · `PurchaseInvoice` (1)

## Bound actions (5)

Invoked as `POST /APINVDET({key})/Model.Entities.<Action>`. Parameters and return types are what
the metadata declares, and every action here is **unverified** unless marked otherwise.

Most actions declare no parameters yet return a `…Dialog` type whose fields read like
inputs. That is EBMS's command-dialog pattern: clients have been seen posting those
fields as the request body (`INVENTRY.GetPrice` with `CustomerId` and `Quantity`). The
metadata does not confirm it, so treat the request shape as unknown until tested.

| Action | Parameters | Returns | Capability |
|---|---|---|---|
| `ShowSerialNumbers` | _none declared_ | object | unverified |
| `ReplaceSerialNumber` | _none declared_ | ITracedItemsHolderReplaceSerialNumberCommandDialog { NewNumber: string(24)?, OldNumber: string(24)? } | unverified |
| `AssignLot` | _none declared_ | ITracedItemsHolderAssignLotCommandDialog { Quantity: decimal(2), LotNumber: string(24)? } | unverified |
| `PrintLabels` | _none declared_ | object | unverified |

Also bound here, as on most EBMS entities: `GetCustomizationDesignerMetadata`.
