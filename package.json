{
  "name": "koble-mcp",
  "version": "0.1.0-rc.5",
  "description": "Thin MCP server over the EBMS (Koble) OData API: it owns authentication, the company allowlist and a few hard guards, and nothing else. Procedure and judgement live in skills.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test",
    "start": "node index.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "0.28.2",
    "postject": "1.0.0-alpha.6",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-beta.4",
    "zod": "^4.4.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/koblesystems/koble-mcp.git"
  },
  "homepage": "https://github.com/koblesystems/koble-mcp",
  "bugs": {
    "url": "https://github.com/koblesystems/koble-mcp/issues"
  },
  "bin": {
    "koble": "cli.js"
  }
}
